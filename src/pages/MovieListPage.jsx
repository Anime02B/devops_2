import React from 'react';
import MovieList from "../components/MovieList";

const MovieListPage = () => {
    return (
        <div>
            <MovieList />
        </div>
    );
};

export default MovieListPage;